# Release artifacts produced by scripts/release.ts
/mac/build/

# Bun / node deps
/mac/node_modules/

# Rust build output for the Alacritty backend's bridge. Cargo.lock IS tracked:
# the Xcode build phase passes --locked so a build is reproducible.
/mac/Vendor/alacritty-bridge/target/

# macOS
.DS_Store

## User settings
xcuserdata/
/mac/Config/Local.xcconfig

## Obj-C/Swift specific
*.hmap

## App packaging
*.ipa
*.dSYM.zip
*.dSYM

## Playgrounds
timeline.xctimeline
playground.xcworkspace

# Coding-agent state and per-tool instruction files. Repository-level guidance
# for agents is AGENTS.md, which every tool reads; a tool-specific file next to
# it would be a second copy to keep in sync. Symlink one here if a tool needs it.
.claude/
CLAUDE.md
