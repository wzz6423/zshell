---
title: 常见问题
description: 关于费用、隐私，以及 Zshell 和你的 shell 是什么关系。
---

<Accordions>

<Accordion title="Zshell 免费吗？">

是的。免费下载，源码基于 PolyForm Noncommercial License 1.0.0 公开，不需要账号，也没有订阅。

</Accordion>

<Accordion title="它会替换我的 shell 吗？">

不会。Zshell 运行的就是你已经在用的 shell——它启动 `$SHELL -l`，不碰你的提示符、别名、函数和
dotfiles。终端窗格可以选择 Ghostty 或 Alacritty 作为后端。

</Accordion>

<Accordion title="它会收集数据吗？">

没有遥测，也没有分析统计。Zshell 自己发起的唯一网络请求，是向 Gitee 上的发布 feed 检查更新，失败时回退 GitHub，而这个可以在 **设置 → Updates** 里关掉。

你在[浏览器窗格](/zh/docs/browser)里打开的网页，以及自己运行的 agent CLI，会像在 Zshell 外面一样发起你要求的网络请求。

</Accordion>

<Accordion title="它会读我的 Ghostty 或 Alacritty 配置吗？">

不会。后端、字体、配色和输入行为都来自 Zshell 自己的[设置](/zh/docs/configuration)，两个终端应用的配置文件都不会加载。

</Accordion>

<Accordion title="退出之后我的会话会怎样？">

项目、标签页和窗格布局会在重启后回来，每个终端会在原目录里以全新的 shell 打开。退出之后不会有任何东西继续在后台跑。

如果你打开 [`terminal.restore-history`](/zh/docs/configuration#terminalrestore-history)，之前的滚动历史也会回来——它以静态文本的形式出现在 `Session Contents Restored` 分隔线上方。

</Accordion>

<Accordion title="这是一个 IDE 吗？">

不是，也不打算往那个方向走。终端始终是核心。git 和文件面板的存在，是为了让你不切换应用就能审阅并提交终端里完成的工作——而不是变成你主要的工作场所。

</Accordion>

<Accordion title="为什么有个对话框问我某个程序能不能读剪贴板？">

因为确实有程序在问。`OSC 52` 转义序列允许终端程序读取系统剪贴板，包括通过 SSH 跑在远程主机上的程序。Zshell 会先跟你确认，而不是悄悄交出去——这和 Ghostty 的默认行为一致。

</Accordion>

<Accordion title="能和 AI 编码工具一起用吗？">

很大程度上就是为此而做的。打开 AI 后，直接用自然语言让受支持的 agent 在后台窗格运行任务，或把范围明确的工作交给另一个 agent。不同 Zshell 窗格里的 agent 还可以互相询问、传递发现和补充要求，不再需要你来回复制上下文。Zshell 会显示 provider 报告的 working、blocked 和完成状态，不会根据终端文字猜测；批准与最终 diff 仍由你决定。见[与 AI Agent 协作](/zh/docs/automation)。

当然也可以把 agent 当作普通终端程序使用：在 [Git 面板](/zh/docs/git)里审阅它的 diff，在旁边读文件，然后只暂存真正想要的部分。

</Accordion>

<Accordion title="在哪里报告 bug？">

在 [GitHub](https://github.com/wzz6423/zshell/issues) 上开一个 issue。写清 macOS 版本，以及出问题时你在做什么，通常就够排查了。

</Accordion>

</Accordions>
