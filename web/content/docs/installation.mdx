---
title: Installation
description: Download Zshell, install it with Homebrew, keep it updated, and remove it cleanly.
---

Zshell uses ad-hoc signing and is not notarized by Apple. [The home page](/en)
shows the minimum macOS version the current
release needs, next to the download.

## Download

Grab the `.dmg` from [the home page](/en), open it, and drag **Zshell** into
your Applications folder.

On first launch, macOS may block the app. After trying to open it, go to
**System Settings → Privacy & Security → Open Anyway**, then confirm opening
Zshell. This also applies to Homebrew installations.

## Homebrew

```bash
brew install wzz6423/tap/zshell
```

The cask lives in `wzz6423/homebrew-tap`, which is why the tap is named in full.
Upgrade the same way you upgrade anything else:

```bash
brew upgrade --cask --greedy-auto-updates zshell
```

## Updates

Zshell detects how it was installed and uses the matching updater. A direct
download uses [Sparkle](https://sparkle-project.org): it checks in the background,
downloads quietly, and installs the new version the next time you quit. A
Homebrew installation updates through Homebrew instead.

To change this, open **Settings → Updates** (<kbd>Cmd</kbd>+<kbd>,</kbd>):

- **Automatically check for updates** — controls Sparkle checks for direct downloads.
- **Check for Updates…** — ask Sparkle to check, or run Homebrew for a cask install.

<Callout title="Installed with Homebrew?">
The Updates section identifies the Homebrew installation and its update button
runs `brew upgrade --cask --greedy-auto-updates zshell`. You can also run that command in Terminal.
</Callout>

## Open a folder from Finder

Zshell registers a macOS service called **New Zshell Project Here**. Select one or
more folders in Finder, right-click, and pick it from the **Services** submenu —
each folder opens as a project with its terminal already started there.

If it's missing from the menu, turn it on under **System Settings → Keyboard →
Keyboard Shortcuts → Services → Files and Folders**. macOS only lists a new
service after the app has been launched once.

## Uninstalling

If **Settings → Automation → AI** is enabled, turn it off before deleting the
app so Zshell can remove the agent support it installed.

Delete `zshell.app` from Applications, then remove its settings if you want a
clean slate:

```bash
rm -rf ~/.config/zshell
```

Window layout, sidebar widths, and the session snapshot live in macOS's own
preference store:

```bash
defaults delete sh.zshell
```
